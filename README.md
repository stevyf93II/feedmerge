# feedmerge
Sync a vendor data feed into a clean, versioned JSON catalog without letting a fast feed wipe slow, hand-built enrichment. Adapter, normalize, merge, publish — with retire-don't-delete records and a publish guard that refuses suspicious record-count drops.
